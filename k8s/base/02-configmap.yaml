apiVersion: v1
kind: ConfigMap
metadata:
  name: stride-config
  namespace: stride
data:
  NODE_ENV:              production
  POSTGRES_USER:         stride
  POSTGRES_DB:           stride
  PORT:                  "4000"

  # ── URLs (override in clusters/stride/kustomization.yaml) ───────────────────
  APP_URL:               https://stride.inferix.in
  FRONTEND_URL:          https://stride.inferix.in
  NEXT_PUBLIC_API_URL:   https://api.stride.inferix.in
  NEXT_PUBLIC_APP_URL:   https://stride.inferix.in

  # ── Email Lambda ─────────────────────────────────────────────────────────────
  EMAIL_LAMBDA_URL:      https://t17ry79c9f.execute-api.ap-south-1.amazonaws.com/

  # ── Contact ──────────────────────────────────────────────────────────────────
  CONTACT_EMAIL:         reharse@inferix.in

  # ── API public URL (used for OAuth callbacks) ────────────────────────────
  API_URL:               https://api.stride.inferix.in

  # ── Redis (BullMQ job queues) ─────────────────────────────────────────────
  REDIS_HOST:            redis-svc
  REDIS_PORT:            "6379"

  # ── Typesense (cloud-hosted) ──────────────────────────────────────────────
  TYPESENSE_HOST:        kmhc0p7r4ef8bugap-1.a1.typesense.net
  TYPESENSE_PORT:        "443"
  TYPESENSE_PROTOCOL:    https

  # ── Stripe ───────────────────────────────────────────────────────────────
  STRIPE_PRICE_PRO:      price_xxx
  STRIPE_PRICE_BUSINESS: price_xxx
